const scrap = require("../../utils/scraperSensa")


const section = document.getElementById("scrapSection")
document.getElementById("rating").addEventListener("click",async function(){
    // const coments = await scrap(movieDetails.title);
    // section.innerHTML = coments
    console.log("clicaste aquí");
})
